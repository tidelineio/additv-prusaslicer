# Upstream PrusaSlicer workflow — disabled in Additv fork.
# Use additv-build.yml instead.
name: Generate release version of flatpak manifest
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled in the Additv fork. Use Additv Build instead."
